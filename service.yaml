apiVersion: v1
kind: Service
metadata:
  name: patient-service-service
  labels:
    app: iit-nginx-app
spec:
  selector:
    app: meditrack-apis
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 80
  type: ClusterIP
